{%- doc -%}
  Buy-buttons styles consolidated into a single snippet.
  Uses a plain CSS asset file instead of {% stylesheet %} so
  the rules are available on every page type — including
  collection and home pages where the quick-add modal fires
  but the buy-buttons block never renders on that page template.

  @example
  {% render 'buy-buttons-styles' %}
{%- enddoc -%}

{{ 'buy-buttons.css' | asset_url | stylesheet_tag }}
